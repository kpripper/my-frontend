interface User {
  name: string
  id: number
}

const user: User = {
  name: 'Hayes',
  id: 0,
}

function summ(a: any) {
  const x = Object.keys(a).map(k => {})

  return summ
}

summ(user)

export {}
